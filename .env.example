# Claude Code OpenAI Wrapper Configuration

# === DOCKER SETTINGS (for Docker deployment) ===

# VNC Password for accessing the desktop GUI (REQUIRED for Docker - CHANGE THIS!)
# Used to access the desktop at http://localhost:6080
# VNC_PASSWORD=your-secure-password-here

# Authentication Method for Docker
# Choose one of: browser, api_key, bedrock, vertex
# AUTH_METHOD=browser

# Display Resolution for Docker GUI
# RESOLUTION=1920x1080x24

# === AUTHENTICATION OPTIONS ===

# For direct API key authentication:
# ANTHROPIC_API_KEY=your-anthropic-api-key

# For AWS Bedrock:
# CLAUDE_CODE_USE_BEDROCK=1
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# For Google Vertex AI:
# CLAUDE_CODE_USE_VERTEX=1
# ANTHROPIC_VERTEX_PROJECT_ID=your-gcp-project-id
# CLOUD_ML_REGION=us-central1

# === GENERAL SETTINGS ===

# Claude CLI Configuration
CLAUDE_CLI_PATH=claude

# API Configuration
# If API_KEY is not set, server will prompt for interactive API key protection on startup
# Leave commented out to enable interactive prompt, or uncomment to use a fixed API key
# API_KEY=your-optional-api-key-here
PORT=8000

# Timeout Configuration (milliseconds)
MAX_TIMEOUT=600000

# CORS Configuration
CORS_ORIGINS=["*"]